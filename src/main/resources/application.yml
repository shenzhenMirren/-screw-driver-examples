spring:
  datasource:
    url: jdbc:mysql://localhost:3306/root?useUnicode=true&useSSL=false&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver    username: root
    password: root
mybatis:
  mapper-locations: classpath:mappers/*Mapper.xml
    